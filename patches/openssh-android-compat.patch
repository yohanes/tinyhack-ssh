--- a/defines.h
+++ b/defines.h
@@ -592,7 +592,7 @@
 # define __attribute__(x)
 #endif /* !defined(__GNUC__) || (__GNUC__ < 2) */
 
-#if !defined(HAVE_ATTRIBUTE__SENTINEL__) && !defined(__sentinel__)
+#if !defined(__ANDROID__) && !defined(HAVE_ATTRIBUTE__SENTINEL__) && !defined(__sentinel__)
 # define __sentinel__
 #endif
 
